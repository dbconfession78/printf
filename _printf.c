#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <limits.h>
#include "holberton.h"

/**
 * _printf - recieves standrd input stream and writes to standard output stream
 * @format: character string that handles the 'c' 's' and '%' directives
 * Return: number of characters printed
 */

int _printf(const char *format, ...)
{
	int result = 0;

	return (result);

}
